# Copy to .env and fill in (never commit .env).
# Variables read by src/config/settings.py — see Settings.from_env().

# Required: OpenAI API key for chat completions + tool calling.
OPENAI_API_KEY=sk-...

# Optional: MCP server URL (Streamable HTTP JSON-RPC). Default matches DEFAULT_MCP_URL in settings.
MCP_SERVER_URL=https://order-mcp-74afyau24q-uc.a.run.app/mcp

# Optional: chat model id. Default matches DEFAULT_MODEL in settings.
OPENAI_MODEL=gpt-4o-mini
